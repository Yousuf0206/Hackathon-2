apiVersion: v1
kind: Service
metadata:
  name: {{ include "recurring-task.fullname" . }}
  labels:
    {{- include "recurring-task.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "recurring-task.selectorLabels" . | nindent 4 }}
